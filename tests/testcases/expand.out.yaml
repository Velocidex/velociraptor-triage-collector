Query: LET _ <= import(artifact="Windows.Triage.Targets")
Output: []

# ExpandPath() extracts the binary which is the first element of the
# command line. There are some weird edge cases with that so we test
# it here. Command Lines may be expanded from environment variables
# and Windows space expansion is inconsistent and unexpected.
Query: SELECT ExpandPath(Path=Path) FROM parse_json_array(data=Cases)
Output: [
 {
  "ExpandPath(Path=Path)": "\\system32\\notepad.exe"
 },
 {
  "ExpandPath(Path=Path)": "c:\\program files\\windows\\notepad.exe"
 },
 {
  "ExpandPath(Path=Path)": "c:\\program files\\windows\\notepad.exe"
 },
 {
  "ExpandPath(Path=Path)": "testenv\\notepad.exe"
 },
 {
  "ExpandPath(Path=Path)": "testenv\\system32\\msiexec.exe"
 },
 {
  "ExpandPath(Path=Path)": "testenv\\notepad.exe"
 }
]

